defaults:
  - _self_
  - runs@run: comparative-1-mistral-7b-v0.3-ptb

mode: full
results_dir: results

wandb:
  entity: airas
  project: 2026-01-28
  mode: online

hydra:
  run:
    dir: ${results_dir}/${hydra.job.name}
  job:
    chdir: true
